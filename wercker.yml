box: wercker/golang

build:
    steps:
        - shellcheck
        - setup-go-workspace@1.0.4
        - script:
            name: prune
            code: |
                rm -rf $WERCKER_ROOT/.git
                rm $WERCKER_ROOT/.gitignore
